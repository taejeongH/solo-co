version: '3'

services:
  redis:
    image: redis:7
    container_name: redis
    
  backend:
    image: solo-backend:latest
    container_name: solo-backend
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    restart: always
    env_file: .env
