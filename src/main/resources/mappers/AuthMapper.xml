<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.mapper.AuthMapper">

    <select id="findByUsername" parameterType="string" resultType="com.ssafy.entity.User">
        SELECT
            user_id,
            username,
            password,
            email,
            name,
            profile_image
        FROM users
        WHERE username = #{username}
    </select>
    
    <insert id="insertUser" parameterType="com.ssafy.entity.User">
	    INSERT INTO users (username, password, email, name, profile_image)
	    VALUES (#{username}, #{password}, #{email}, #{name}, #{profileImage})
	</insert>
</mapper>