<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.travel.community.mapper.ProjectPostCommentMapper">

    <!-- 게시글 댓글 목록 조회 -->
    <select id="findCommentsByPostId" resultType="com.ssafy.travel.community.dto.CommentDto">
        SELECT
            c.comment_id AS commentId,
            c.user_id AS userId,
            u.name AS userName,
            u.profile_image AS userProfileImage,
            c.content AS content,
            DATE_FORMAT(c.created_at, '%Y-%m-%d %H:%i') AS createdAt
        FROM project_post_comment c
        JOIN users u ON c.user_id = u.user_id
        WHERE c.post_id = #{postId}
        ORDER BY c.created_at ASC
    </select>
    
    <delete id="deleteCommentsByPostId">
    	DELETE FROM project_post_comment WHERE post_id=#{postId}
    </delete>

</mapper>
