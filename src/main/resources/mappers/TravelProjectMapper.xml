<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.travel.project.mapper.TravelProjectMapper">

    <select id="findProjectsByUserIdAndType" resultType="com.ssafy.travel.project.entity.TravelProject">
	    SELECT
	        tp.project_id   AS projectId,
	        tp.owner_id     AS ownerId,
	        tp.title        AS title,
	        tp.location     AS location,
	        tp.start_date   AS startDate,
	        tp.end_date     AS endDate,
	        tp.project_type AS projectType,
	        tp.created_at   AS createdAt,
	        tp.thumbnail    AS thumbnail
	    FROM travel_project tp
	    LEFT JOIN project_member pm ON tp.project_id = pm.project_id
	    WHERE pm.user_id = #{userId}
	    
	    <if test ="projectType != null">
	    	AND tp.project_type = #{projectType}
	    </if>
	    
	</select>
	
	<insert id="createProject" useGeneratedKeys="true" keyProperty="projectId">
	    INSERT INTO travel_project
	    (owner_id, title, location, start_date, end_date, project_type, thumbnail)
	    VALUES
	    (#{ownerId}, #{title}, #{location}, #{startDate}, #{endDate}, #{projectType}, #{thumbnail})
	</insert>
	

    <select id="findById" parameterType="long" resultType="com.ssafy.travel.project.entity.TravelProject">
        SELECT
	        tp.project_id   AS projectId,
	        tp.owner_id     AS ownerId,
	        tp.title        AS title,
	        tp.location     AS location,
	        tp.start_date   AS startDate,
	        tp.end_date     AS endDate,
	        tp.project_type AS projectType,
	        tp.created_at   AS createdAt,
	        tp.thumbnail    AS thumbnail
        FROM travel_project tp
        WHERE project_id = #{projectId}
    </select>
    
	<update id="update" parameterType="com.ssafy.travel.project.entity.TravelProject">
	    UPDATE travel_project
	    SET 
	        title = #{title},
	        location = #{location},
	        start_date = #{startDate},
	        end_date = #{endDate},
	        project_type = #{projectType},
	        thumbnail = #{thumbnail}
	    WHERE project_id = #{projectId};
	</update>
	
	<delete id="delete">
	    DELETE FROM travel_project WHERE project_id = #{projectId}
	</delete>
	

</mapper>
