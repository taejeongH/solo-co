<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.mapper.UserMapper">

    <!-- 회원 조회 -->
    <select id="findById" resultType="com.ssafy.entity.User">
        SELECT id, email, password, nickname, profile
        FROM users
        WHERE id = #{id}
    </select>

    <!-- 회원 수정 -->
    <update id="updateUser">
        UPDATE users
        SET 
            nickname = #{nickname},
            profile = #{profile},
            password = #{password}
            email = #{email}
        WHERE id = #{id}
    </update>

</mapper>
