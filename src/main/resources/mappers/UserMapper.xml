<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.user.mapper.UserMapper">

    <!-- 회원 조회 -->
    <select id="findById" resultType="com.ssafy.auth.entity.User">
        SELECT user_id as userid, username, password, email, name, profile_image as profileImage
        FROM users
        WHERE user_id = #{userId}
    </select>

    <!-- 회원 수정 -->
    <update id="updateUser">
        UPDATE users
        SET 
        	name = #{name},
            profile_image = #{profileImage},
            password = #{password},
            email = #{email}
        WHERE user_id = #{userId}
    </update>
    
    <!-- 회원 삭제 -->
    <delete id="deleteUser" parameterType="long">
	    DELETE FROM users
	    WHERE user_id = #{userId}
	</delete>

</mapper>
